{
  "last_updated": "2026-07-08T12:35:00Z",
  "lessons": [
    {
      "date": "2026-07-08",
      "context": "BGE-M3 model loading hang during quality check",
      "lesson": "First-time ML model loads (SentenceTransformer, PyTorch, etc.) can block for 30-120+ seconds with NO logging, appearing as a program hang. The BGE-M3 adequacy gate triggers this load on the first chunk's quality check. Users see 'Step 5/7: Checking quality' then nothing for minutes. RAG retriever had this logging ('Loading BGE-M3 query embedder (first use, may take a minute)...') but the adequacy gate's independent load path did not.",
      "action": "Always add progress logging for model loads and long-running operations. Use timing logs (time.time() before/after) to help diagnose performance issues. Log at INFO level for user-facing operations."
    },
    {
      "date": "2026-07-08",
      "context": "Model prompt analysis — reviewing all output files for a-will-eternal",
      "lesson": "Prompt system is architecturally correct but failures are OUTPUT FORMATTING issues, not prompt issues. qwen3.5-9b leaks thinking blocks (``), translategemma wraps in code blocks (```), gemma4 leaks ${} template vars. The fix is POSTPROCESSOR, not prompt rewrite. Sailor2 persona override needs to be stronger (model ignores weak instructions).",
      "action": "Add postprocessor strips: (1) `` blocks for qwen3, (2) ``` blocks for translategemma, (3) ${} templates for gemma4. Strengthen sailor2 prompt with explicit 'NEVER output English' instruction."
    },
    {
      "date": "2026-07-07",
      "context": "qwen3.5-9b translation failure — truncated output, English leakage, HTTP 500",
      "lesson": "Prompt budget is SCARCE on small models. Qwen3.5-9b with 16K context got a 13K-char prompt (full ENGLISH_TRANSLATOR_SYSTEM_PROMPT + rules + patch) which overwhelmed it. Result: truncated output, 856 English words in Myanmar output, format crashes. Fixed by creating SHORT prompt (4K chars) with only essential rules. Each model family needs a differently-sized prompt tuned to its context window.",
      "action": "Always check prompt size vs model context window. For 9B models: max 4K chars system prompt. For 27B models: max 8K chars. For 128K+ models (Gemma4): full prompt OK."
    },
    {
      "date": "2026-06-16",
      "context": "EN→MM quality review a-will-eternal1 Chapter 1",
      "lesson": "padauk-gemma at temp=0.2 produces 98.9% Myanmar ratio and 96/100 fluency but cannot maintain character-epithet consistency across chunks without glossary injection. 'Weasel' rendered as 3 different animals, name bar had 2 variants. Glossary enforcement in translator.py is advisory (text block) not structural (term table). RAG with human pairs would anchor consistent style.",
      "action": "Enable RAG before multi-chapter runs. Add recurring epithets/nicknames to glossary. Postprocessor auto-correct known name variants. Current chunk_size 1500 may be too large — 1200 recommended for padauk-gemma."
    },
    {
      "date": "2026-05-31",
      "context": "Dead code cleanup across project",
      "lesson": "Large sections of cn_mm_rules.py / en_mm_rules.py structured dicts (VOCABULARY_PRECISION, CULTURAL_RULES, CONFRONTATION_SPEECH_PATTERN) were dead code — imported but never read at runtime. The cultural_injector.py now bridges them. Always verify that structured data dicts are actually consumed at runtime, not just imported.",
      "action": "Run grep for dict imports vs dict .get() calls at runtime. Any dict that's only imported but never .get()-ed in production code is dead."
    },
    {
      "date": "2026-05-31",
      "context": "FastTranslator/FastRefiner dead code",
      "lesson": "FastTranslator and FastRefiner were created as optimized alternatives but never wired into orchestrator. The Translator class directly handles both normal and fast paths. Avoid creating 'alternative' classes that aren't wired into the pipeline.",
      "action": "Single Translator/Refiner class with config-driven behavior is sufficient."
    },
    {
      "date": "2026-05-31",
      "context": "PivotTranslator dead code",
      "lesson": "PivotTranslator (CN→EN→MM) was built but the pipeline uses settings.pivot.yaml with two Translator calls, not the PivotTranslator class. The two-stage pivot is a pipeline configuration, not a separate agent.",
      "action": "Pivot translation = two sequential Translator calls with different configs. No separate agent class needed."
    },
    {
      "date": "2026-05-31",
      "context": "RAG system",
      "lesson": "RAG is wired into the codebase (rag_retriever, feedback_loop, ChromaDB) but OFF by default (config rag: {}). It works when enabled.",
      "action": "RAG is available. Document how to enable it in settings.yaml (rag.enabled: true)."
    },
    {
      "date": "2026-06-14",
      "context": "RAG human-corpus",
      "lesson": "A whole RAG pipeline can be silently dead: ChromaDB empty + a scale-mismatched filter meant the 1315-chapter human corpus never reached the model. Verify retrieval returns rows end-to-end, not just that ingestion 'ran'.",
      "action": "After any RAG change, assert retrieve_similar() returns >0 expected rows."
    },
    {
      "date": "2026-06-14",
      "context": "Glossary taxonomy",
      "lesson": "category column was free-text (no CHECK), so richer categories needed no migration — only subtype (additive column) + a relationship edge table did. Check column constraints before assuming a migration is required.",
      "action": "PRAGMA table_info + look for CHECK before planning schema work."
    },
    {
      "date": "2026-06-14",
      "context": "Auto-seed vs tests",
      "lesson": "Making MemoryManager auto-seed 157 global terms on every init perturbed count/parity tests and exposed a test that wrote to the REAL default DB. Gate new always-on startup behavior behind a flag (auto_seed_global) so tests can opt out.",
      "action": "New startup side-effects get a default-on flag; tests pass it False and use isolated db_path."
    },
    {
      "date": "2026-06-14",
      "context": "opencode review triage",
      "lesson": "External AI review (deepseek) flagged 10 issues; 2 were false positives (rapidfuzz 'missing' though in requirements; a deliberate heuristic) and 1 mischaracterized a no-op as a crash. Always verify each finding against the actual code before fixing.",
      "action": "Triage every external-review finding; reproduce before fixing; record rejected ones."
    }
  ],
  "known_patterns": [
    {
      "pattern": "Dead code accumulates in structured rule dicts and unused agent classes",
      "cause": "Rules are written as structured data but never connected to the runtime code path",
      "fix": "Use cultural_injector.py pattern: import dicts at module level, match at runtime in a single function call"
    },
    {
      "pattern": "Async/sync replacement classes left as dead files",
      "cause": "Developer creates new implementation (FastTranslator) but leaves old code path unchanged",
      "fix": "Either wire the new class into the pipeline or delete it. Dead files confuse maintainers."
    }
  ],
  "model_performance": {
    "padauk-gemma:q8_0": {
      "avg_quality_score": 82.0,
      "chapters_tested": 35
    },
    "gemma4-e4b-it:q8_0": {
      "avg_quality_score": 85.0,
      "chapters_tested": 1,
      "notes": "Clean output, minor ${} template leak"
    },
    "qwen3.5-9b:latest": {
      "avg_quality_score": 0,
      "chapters_tested": 1,
      "notes": "BROKEN: thinking leak, infinite repetition loop, debug output leaked"
    },
    "sailor2-20b-chat.q4:latest": {
      "avg_quality_score": 0,
      "chapters_tested": 1,
      "notes": "REJECTED: English words, Myanmar ratio only 36-40%"
    },
    "translategemma-simple:latest": {
      "avg_quality_score": 0,
      "chapters_tested": 1,
      "notes": "PARTIAL: code block contamination, empty parentheses"
    }
  }
}